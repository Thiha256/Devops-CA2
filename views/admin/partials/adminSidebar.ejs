<aside class="bg-white border rounded-4 p-3 admin-sidebar" style="width:190px; flex-shrink:0; position:sticky; top:90px;">

    <div class="mb-4">
        <p class="text-uppercase text-muted small fw-bold mb-2" style="font-size:10px; letter-spacing:1px;">Main Menu</p>

        <a href="/admin" class="d-flex align-items-center gap-2 px-2 py-2 mb-1 rounded-3 text-decoration-none fw-semibold small <%= page === 'inventory' ? 'bg-primary text-white' : 'text-primary' %>">
            <i class="fa-solid fa-boxes-stacked"></i> Manage Inventory
        </a>

        <a href="/admin/loans" class="d-flex align-items-center gap-2 px-2 py-2 mb-1 rounded-3 text-decoration-none fw-semibold small <%= page === 'loans' ? 'bg-primary text-white' : 'text-primary' %>">
            <i class="fa-solid fa-clipboard-list"></i> Active Loans
        </a>

        <a href="/admin/reports" class="d-flex align-items-center gap-2 px-2 py-2 mb-1 rounded-3 text-decoration-none fw-semibold small <%= page === 'reports' ? 'bg-primary text-white' : 'text-primary' %>">
            <i class="fa-solid fa-chart-column"></i> Reports
        </a>
    </div>

    <div>
        <p class="text-uppercase text-muted small fw-bold mb-2" style="font-size:10px; letter-spacing:1px;">Account</p>

        <a href="/admin/profile" class="d-flex align-items-center gap-2 px-2 py-2 mb-1 rounded-3 text-decoration-none fw-semibold small <%= page === 'profile' ? 'bg-primary text-white' : 'text-primary' %>">
            <i class="fa-solid fa-user-shield"></i> Admin Profile
        </a>

        <div class="bg-light border rounded-3 p-3 mt-3">
            <h6 class="text-success mb-1" style="font-size:13px;">Resource Centre</h6>
            <p class="text-muted mb-0" style="font-size:11.5px;">
                Mon - Fri<br>
                8:30AM - 6:00PM
            </p>
        </div>
    </div>

</aside>