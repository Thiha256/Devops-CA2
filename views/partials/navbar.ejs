<nav class="navbar navbar-expand bg-white border-bottom sticky-top py-2 px-4">
    <div class="d-flex align-items-center gap-3">
        <img src="/images/rp-logo.png" alt="Republic Polytechnic Logo" style="height:42px;">
        <div>
            <h5 class="mb-0 fw-bold">RP Resource Centre</h5>
            <small class="text-success fw-semibold">Republic Polytechnic</small>
        </div>
    </div>

    <form class="mx-auto" style="width:420px;" action="/browse" method="GET" role="search">
        <div class="input-group">
            <span class="input-group-text bg-light border-end-0"><i class="fa-solid fa-magnifying-glass text-muted"></i></span>
            <input type="text" name="q" value="<%= typeof query !== 'undefined' ? query : '' %>" class="form-control bg-light border-start-0" placeholder="Search laptops, cameras, chargers...">
        </div>
    </form>

    <div class="d-flex align-items-center gap-3">
        <a href="/notifications" class="btn btn-light btn-sm rounded-circle border position-relative d-flex align-items-center justify-content-center" style="width:38px;height:38px;" title="Notifications">
            <i class="fa-regular fa-bell"></i>
            <% if (typeof unreadCount !== 'undefined' && unreadCount > 0) { %>
                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size:9px;">
                    <%= unreadCount %>
                    <span class="visually-hidden">unread notifications</span>
                </span>
            <% } %>
        </a>
        <button class="btn btn-light btn-sm rounded-circle border" style="width:38px;height:38px;">
            <i class="fa-regular fa-envelope"></i>
        </button>
        <div class="d-flex align-items-center gap-2">
            <div class="rounded-circle bg-success bg-opacity-10 text-success d-flex align-items-center justify-content-center" style="width:38px;height:38px;">
                <i class="fa-solid fa-user"></i>
            </div>
            <div>
                <div class="fw-semibold small"><%= student.name %></div>
                <div class="text-muted" style="font-size:11px;"><%= student.id %></div>
            </div>
        </div>
        <!-- ==================================================
             Devops-CA2: AI Chatbot Support Integration
             Implemented by: Paul Chin
             ================================================== -->
        <a href="https://cdn.botpress.cloud/webchat/v3.6/shareable.html?configUrl=https://files.bpcontent.cloud/2026/05/12/01/20260512013415-ZHY61519.json" 
           target="_blank" 
           rel="noopener noreferrer"
           class="btn btn-primary btn-sm rounded-pill fw-bold"
           style="font-family: sans-serif;">
           💬 Live Chat
        </a>
        <a href="/logout" class="btn btn-outline-primary btn-sm rounded-pill px-3">Logout</a>
    </div>
</nav>

<script src="/js/rp-ui.js"></script>