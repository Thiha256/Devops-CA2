# Copy this file to `.env` and fill in your local MySQL credentials.
# `.env` is gitignored so your real password is never committed.
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_DATABASE=resource_centre_db
