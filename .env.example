# Copy this file to `.env` and fill in your local MySQL credentials.
# `.env` is git-ignored and must never be committed.
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=resource_centre_db
